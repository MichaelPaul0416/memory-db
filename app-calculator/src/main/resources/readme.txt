1.计算节点，调用rpc服务从h2数据库中获取数据，并计算
2.将计算结果写入本地磁盘文件
3.calculator启动时，每个节点将自己读取那几张表注册到h2中，然后各个节点的dataProcessor就知道pageData中应该去哪些表查那几张数据--尚未实现'
4.每个node在计算完数据发送给MQ之后，需要发送一条计算完毕的消息（包含节点名称）给MQ，然后写入节点监听这个消息，当得知所有节点都计算完或者超时之后，就退出程序
5.先启动写数据的节点